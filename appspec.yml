version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: your-task-definition
        LoadBalancerInfo:
          ContainerName: your-container-name
          ContainerPort: 80
